<UserControl x:Class="OrdersApp.Tabs.FindEmployee"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OrdersApp.Tabs"
             xmlns:vm="clr-namespace:OrdersApp.ViewModels"
             mc:Ignorable="d" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DataContext="{d:DesignInstance vm:FindEmployeeViewModel}"
             d:DesignHeight="800" d:DesignWidth="1500">
    <Border Background="White" CornerRadius="20" Margin="20" Grid.Column="1">
        <ScrollViewer>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="textBoxPayment"             Grid.Column="1" Grid.Row="1" materialDesign:HintAssist.Hint="Payment"                            Margin="20 50 20 50"/>
                <TextBox x:Name="textBoxDescription"         Grid.Column="1" Grid.Row="2" materialDesign:HintAssist.Hint="Description"                        Margin="20 50 20 50"/>
                <TextBox x:Name="textBoxTelephoneNumber"     Grid.Column="1" Grid.Row="3" materialDesign:HintAssist.Hint="Telephone number, e.g. 83335552222" Margin="20 50 20 50"/>
                <TextBox x:Name="textBoxCountOfPeopleNeeded" Grid.Column="1" Grid.Row="4" materialDesign:HintAssist.Hint="Count of people needed"             Margin="20 50 20 50"/>

                <Button x:Name="addOrder"                    Grid.Column="1" Grid.Row="5" Content="Create the order" Click="addOrder_Click"  Height="50" Margin="20"/>

                <materialDesign:Snackbar x:Name="snackBar"   Grid.Column="1" Grid.Row="7" FontFamily="{DynamicResource MaterialDesignFont}"  FontSize="25"  IsActive="False" MessageQueue="{materialDesign:MessageQueue}" Height="70" Margin="20"/>

            </Grid>
        </ScrollViewer>
    </Border>
</UserControl>
